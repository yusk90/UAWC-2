(function (app) {
    app.MenuWidgetView = app.BaseWidgetView.extend({
        tagName: 'nav',
        className: 'menu',
        initialize: function () {
            // TODO
        }
    })
})(app);